{% extends 'admin:layout/page.twig' %}

{% block title %}{{ resource.title | trans }}{% endblock %}

{% block body %}
    <div class="sf-heading">
        {{ admin_panel_breadcrumbs(resource.breadcrumbs) }}
        <h1>{{ resource.title | trans }}</h1>
    </div>
    <div class="sf-main__wrapper">
        {% block resource %}{% endblock %}
    </div>
{% endblock %}
